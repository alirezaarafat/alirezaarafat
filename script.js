const pages=[...document.querySelectorAll('.page')];const links=[...document.querySelectorAll('.page-link')];const nav=document.getElementById('links');const menu=document.getElementById('menu');
function showPage(id,push=true){const target=document.getElementById(id)||document.getElementById('home');pages.forEach(p=>p.classList.remove('active-page'));target.classList.add('active-page');links.forEach(l=>l.classList.toggle('active',l.dataset.page===target.id));nav.classList.remove('open');window.scrollTo({top:0,behavior:'smooth'});if(push)history.pushState({page:target.id},'',`#${target.id}`)}
links.forEach(l=>l.addEventListener('click',()=>showPage(l.dataset.page)));menu.addEventListener('click',()=>nav.classList.toggle('open'));window.addEventListener('popstate',()=>showPage(location.hash.slice(1)||'home',false));document.getElementById('year').textContent=new Date().getFullYear();showPage(location.hash.slice(1)||'home',false);
